<h2>Страница стоянки</h2>
{{#if  nomers.length}}
  <ul>
    {{#each nomers}}
    <li>
      {{#if arrivalDate}}
        <span>Номер машины: {{nomer}};</span>
        <span>время въезда: {{arrivalDate}};</span>
      {{else}}
        нет номера
      {{/if}}
    </li>
    {{/each}}
  </ul>
{{else}}
<p>Нет Машин на стоянке!</p>

{{/if}}